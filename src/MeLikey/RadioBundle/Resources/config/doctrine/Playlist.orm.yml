MeLikey\RadioBundle\Entity\Playlist:
    type: entity
    id:
        id:
            type: integer
            generator: { strategy: AUTO }
    fields:
        name:
            type: string
            nullable: false
    oneToMany:
        playlistItems:
            targetEntity: PlaylistItem
            mappedBy: playlist
            orderBy:
                position: DESC
